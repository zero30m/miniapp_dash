# 首页 UI 改造方案文档

## 1. 概述 (Overview)
本已阅读并分析了当前小程序首页代码及 reference 的 `UI_home.html` 文件。本方案旨在将现有的“黑暗赛博朋克/故障艺术”风格（Dark/Cyberpunk/Glitch）改造为 `UI_home.html` 中展示的“**高饱和多巴胺/新丑主义（Neo-Brutalism）**”风格，同时**严格保留现有的页面结构和文案**。

**核心设计语言：**
*   **高亮背景**：使用奶油色背景代替纯黑背景。
*   **粗框线 (Chunky Border)**：所有元素使用统一的 4px 黑色实线边框。
*   **硬阴影 (Hard Shadow)**：取消发光模糊阴影，改为黑色实心位移阴影。
*   **高饱和色块**：使用红、蓝、绿、黄等高饱和度纯色填充。
*   **极简排版**：使用粗体、大写、无衬线字体。

---

## 2. 全局样式规范 (Global Styles)

### 2.1 配色方案 (Color Palette)
请在 `app.wxss` 或页面变量中定义以下新颜色，替换原有的 Neon Green/Black 体系：

*   **页面背景色 (Background)**: `#FFFDE7` (奶油黄) —— *替换原 `#050505`*
*   **主边框/文字色 (Border/Text)**: `#000000` (纯黑)
*   **主要色块 (Accents)**:
    *   🔴 红 (Red): `#FF5252`
    *   🔵 蓝 (Blue): `#448AFF`
    *   🟢 绿 (Green): `#4CAF50`
    *   🟡 黄 (Yellow): `#FFEB3B`
    *   🟣 紫 (Purple): `#9C27B0`

### 2.2 通用组件样式 (Common Components)
所有卡片类元素（Banner、最近玩过、道具卡片）需应用以下通用样式：
*   **边框 (Border)**: `4px solid #000000`
*   **圆角 (Border Radius)**: 建议统一为 **12rpx** 或 **16rpx** (保持一定的秩序感，贴合硬朗风格)。
*   **阴影 (Shadow)**:
    *   大阴影: `box-shadow: 6px 6px 0px 0px #000000;`
    *   小阴影: `box-shadow: 4px 4px 0px 0px #000000;`
*   **字体**: 优先使用无衬线粗体 (Sans-serif Bold/Black)。

---

## 3. 页面模块改造及其细节 (Module Details)

### 3.1 页面容器 (Container)
*   **当前**: 深色背景 `#050505`。
*   **修改目标**:
    *   `background-color`: 修改为 `#FFFDE7`。
    *   移除背景中的网格动画或深色纹理。

### 3.2 Banner 区域 (原 Swiper)
*   **当前**: `banner-swiper`, `banner-card` (圆角40rpx, 发光阴影, 渐变蒙层)。
*   **修改目标**:
    *   **保持原有结构**：不新增标题，不改变布局。
    *   **风格调整**：
        *   **背景**: 改为实体黄色 `#FFEB3B` (或根据 banner 数据配置颜色)。
        *   **边框**: 增加 `4px solid #000000`。
        *   **阴影**: `box-shadow: 6px 6px 0px 0px #000000`。
        *   **圆角**: 由 `40rpx` 调整为 `16rpx`，以匹配硬朗风格。
    *   **内容细节**:
        *   移除 `.banner-overlay` (在波普风格中，通常由纯色背景+文字构成)。
        *   **文字**: 颜色改为 `#000000`，字体加粗，大小调整，移除文字阴影 (text-shadow)。
        *   **按钮**: 保持现有位置，样式改为黑色背景、白色文字、大写粗体、直角或微圆角。

### 3.3 最近玩过 (Recently Played)
*   **当前**: `.recent-capsule` (胶囊形，玻璃拟态，镭射边框)。
*   **标题**: 保持中文 "**最近玩过**"，样式改为黑色粗体。
*   **卡片样式 (`.recent-capsule`)**:
    *   **形状**: 由长条胶囊改为 **正方形** (类似 UI_home 的 Old Man/Dice/Check 卡片)。
    *   **背景**: 统一为 **白色 (`#FFFFFF`)**。
    *   **边框**: `border: 4px solid #000000`。
    *   **阴影**: `box-shadow: 4px 4px 0px 0px #000000`。
    *   **布局**: 垂直排列 (Flex Column)。
        *   **上部**: 图标容器。圆形或方形背景 (红/蓝/绿)，带黑色边框，内部放 icon。
        *   **下部**: 保持中文标题 (如 "抓手指")，黑色粗体。
    *   **移除**: 移除玻璃光泽 (`.capsule-glass`)、噪点纹理。

### 3.4 道具列表 (Tools List)
*   **当前**: `.tool-card-horizontal` (横向卡片, 玻璃拟态, 渐变背景)。
*   **标题**: 保持中文 "**喝酒道具**"，样式改为黑色粗体。
*   **卡片样式 (`.tool-card-horizontal`)**:
    *   **背景**: 不再使用渐变，而是根据道具类型分配实色背景 (Blue `#448AFF`, Red `#FF5252`, Green `#4CAF50`, etc.)。
    *   **边框**: `border: 4px solid #000000`。
    *   **阴影**: `box-shadow: 6px 6px 0px 0px #000000`。
    *   **按压效果 (`:active`)**: `transform: translate(4px, 4px); box-shadow: none;` (实现硬核的按压反馈)。
    *   **圆角**: 统一调整为 `16rpx`。
*   **内部元素**:
    *   **标题/描述**: 颜色改为 **白色** (在深色块上) 或 **黑色** (如果背景较浅)，保持高对比度。字体极粗。
    *   **图标区域 (硬边缘破框)**:
        *   **无衬底设计**: 不添加任何背景容器或几何图形，直接保持银色物体的独立悬浮感。
        *   **硬阴影处理 (Hard Drop Shadow)**: 直接对图片元素应用**黑色实心投影** (参考 `UI_home` 中的 `.paper-cut-icon` 样式：`filter: drop-shadow(4px 4px 0px #000000)`)。
        *   **效果目标**: 让银色物体呈现出类似“厚实贴纸”或“剪纸”的视觉效果，通过强烈的黑色投影将其与彩色背景分离，既保留了破框的动感，又通过硬阴影融入新丑主义风格。
    *   **Tag (Chip)**: 样式改为黑底白字或白底黑字带边框，移除半透明效果。

### 3.5 闪屏 (Splash Screen)
*   **建议**: 鉴于整体风格大变，原有的“赛博故障风”闪屏可能显得突兀。
*   **修改思路**:
    *   背景改为奶油黄 `#FFFDE7`。
    *   Logo 使用黑色版本，带 4px 偏移的彩色(红/蓝)残影。
    *   加载条改为粗黑框+实色填充。

---

## 4. 总结 (Summary)
本次改造的核心在于**去除所有“软”的视觉元素**（渐变、模糊、半透明、发光），全面拥抱**“硬”的视觉元素**（实色、黑边、位移阴影），同时**完全保留现有的功能入口、布局逻辑和中文文案**。
